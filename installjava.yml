---
- hosts : mywebservers
  become : yes
  tasks :
  - name : install java on amazon redhat Nodes
    yum :
      name : "{{javapackgforredhat}}"
      state : present
      

  -  name : Install java on debian node 
     apt :
      name : "{{javapackgfordebian}}"
      state : present
     

