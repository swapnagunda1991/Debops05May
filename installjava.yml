---
- hosts : mywebservers
  become : yes
  tasks :
  - name : install java on amazon redhat Nodes
    yum :
      name : "{{javapackgforredhat}}"
      state : present
      vars:
        use_backend: yum
    
     

